//npm i -D @types/jest @types/supertest jest supertest ts-jest



/*
how to jest

להשתמש בטרי קטץ כשרוצים לוודא איזה שגיאה חוזרת 
   



 stub / mock
 currnet class / static class / instance of class
 all of it / just a function




/**
 * how to stub all of static class
 */

/**
 * how to stub all of instance of class
    class Container {
        contains(item) {
        ...
        }
    }
    var stubContainer = sinon.createStubInstance(Container);
    stubContainer.contains.returns(false);
    stubContainer.contains.withArgs("item").returns(true);
 */

/**
 * how to stub a function of currnet class
    stub(YourClass, 'yourClassMethod').callsFake(() => { return '' })
 */

/**
 * how to stub a function of static class
 */

/**
 * how to stub a function of instance of class
    sinon.stub(YourClass.prototype, 'yourClassMethod').callsFake(() => { return '' })
 */

/**
 * how to mock all of static class
 */

/**
 * how to mock all of instance of class
 */

/**
 * how to mock a function of currnet class
 */

/**
 * how to mock a function of static class
 */

/**
 * how to mock a function of instance of class
 */









/**
 * general pre-commit tasks
 * ------------------------
 * make sure no TODOs left (other then ones supposed to be on a different ticket)
 * make sure all logs are exist and correct
 * no un needed 'import's
 */


/**
 * דברים ללמוד
 * -----------
 * להמשיך עם הקלין קוד
 * להתחיל להעלות לענן ולראות כמה דברים עולים
 * terraform
 * 


עבודה

אנקל בוב

- [ ] סטנדרט של הרצה בצוות, סקריפט אוטומטי מgit clone ועד רצץ של בדיקות מהפוסט מן.
- [ ] ברמת החברה - דרך סטנדרטית לעבודה מול סרביסים חיצוניים, או שמוצאים דרך לשתף את הקונפיג של כל אחד מהצוותים וגם לשתף עידכונים + הנטוורקינג הלוקאלי זהה לנטוורקינג בקלאסטר / יודעים בדיוק מה ההבדלים - או שכולם עובדים מול הintegraion של כולם
- [ ] מבחינצת הלוגר - אולי להוסיף מערך של סשנים, לשמור את כל הלוגים בסשן, ולהדפיס אותם לפי תנאים מסויימים







 */


